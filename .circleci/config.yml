version: 2
jobs:
  #go-latest
  golang-latest:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: 
          name: build capsul image
          command: docker build -t medtune/beta-platform:beta-linux-v0.2.0 .

workflows:
  version: 2
  build:
    jobs:
      - golang-latest