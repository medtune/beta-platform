version: 1

UID: 000000000
name: platform
engine: docker
storage: hub.docker.com
secrets:
  env: $DOCKER_TOKEN

process:
  - package : .
    name: beta-platform
    version : v0.0.4
    tag-format: $(%name-%version)

    build:
      file : Dockerfile

    push:
      org: medtune
      repo: platform
