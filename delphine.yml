version: '1'

UID: '0x00000000'
name: platform
engine: docker
storage: hub.docker.com
secrets:
  env: $DOCKER_TOKEN

process:
  - package : .
    name: beta
    version : v0.0.3
    tag-format: $(%name-%version)

    build:
      file : Dockerfile

    push:
      org: medtune
      repo: platform
